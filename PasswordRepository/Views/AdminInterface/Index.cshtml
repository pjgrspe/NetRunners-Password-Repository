    @model PasswordRepository.Models.PasswordEntryModel
@using PasswordRepository.Helpers;
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html lang="en">
<head>
    <title>Home</title>
    <link href="~/Style/StyleSheetButtonEditTrash.css" rel="stylesheet">
    <link href="~/Style/StyleSheetAdminInterface.css" rel="stylesheet">
</head>
<body>
    <!--Navbar-->
    @Html.Partial("_NavbarDashboard")

    <div id="outer">
        <!-- Modal -->
        <div class="modal fade" id="addbutton-modal" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="ModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered modal-lg">
                <div class="modal-content">
                    <div class="modal-header">
                        <h1 class="modal-title fs-5" id="ModalLabel">ADD NEW USER</h1>
                        <button type="button" class="btn-close btn-close-white" onclick="discardChangesADDITEM()"></button>
                    </div>
                    <!--Form-->
                    <div class="modal-body modal-xl" style="margin-top:-10px;">
                        @using (@Html.BeginForm("UserEntry", "AdminInterface", FormMethod.Post))
                        {
                            @Html.AntiForgeryToken()
                            <div class="registration-form container d-flex justify-content-center" style="text-align:center;">
                                <div class="d-flex justify-content-center w-100">
                                    <div class="form-floating" style="margin-top: 10px; width: 90%;">
                                        @Html.TextBoxFor(Model => Model.textbox_UE_FNAME, new { @class = "form-control rounded-0 w-100", id = "floatingTitle", placeholder = "First Name", required = "required" })
                                        @*<input type="text" maxlength="50" class="form-control rounded-0 w-100" id="floatingTitle" spellcheck="false" placeholder="First Name" required="required">*@
                                        <label for="floatingTitle">First Name</label>
                                    </div>
                                </div>
                                <div class="d-flex justify-content-center w-100">
                                    <div class="form-floating" style="margin-top: 10px; width: 90%;">
                                        @Html.TextBoxFor(Model => Model.textbox_UE_LNAME, new { @class = "form-control rounded-0 w-100", id = "floatingTitle", placeholder = "Last Name", required = "required" })
                                        @*<input type="text" maxlength="50" class="form-control rounded-0 w-100" id="floatingTitle" spellcheck="false" placeholder="Last Name" required="required">*@
                                        <label for="floatingTitle">Last Name</label>
                                    </div>
                                </div>
                                <div class="d-flex justify-content-center w-100">
                                    <div class="form-floating" style="margin-top: 10px; width: 90%;">
                                        @Html.TextBoxFor(Model => Model.textbox_UE_USERNAME, new { @class = "form-control rounded-0 w-100", id = "floatingUsername", placeholder = "Username" })
                                        @*<input maxlength="50" class="form-control rounded-0 w-100" id="floatingUsername" spellcheck="false" placeholder="Username">*@
                                        <label for="floatingUsername">Username</label>
                                    </div>
                                </div>
                                <div class="d-flex justify-content-center w-100">
                                    <div class="form-floating" style="margin-top: 10px; width: 90%; ">
                                        @Html.TextBoxFor(Model => Model.textbox_UE_EMAIL, new { @class = "form-control rounded-0 w-100", id = "floatingEmail", type = "First Name", placeholder = "Email" })
                                        @*<input type="email" maxlength="50" class="form-control rounded-0 w-100" id="floatingEmail" spellcheck="false" placeholder="Email">*@
                                        <label for="floatingEmail">Email</label>
                                    </div>
                                </div>
                                <div class="d-flex justify-content-center w-100">
                                    <div class="form-floating" style="margin-top: 10px; width: 90%;">
                                        @Html.PasswordFor(Model => Model.textbox_UE_PASSWORD, new { @class = "form-control rounded-0 w-100", id = "floatingPassword-login", placeholder = "Password", required = "required" })
                                        @*<input type="password" maxlength="50" class="form-control rounded-0 w-100" id="floatingPassword-login" spellcheck="false" placeholder="Password" required="required">*@
                                        <label for="floatingPassword-login">Password</label>
                                        <span>
                                            <!--Eye icon-->
                                            <img src="~/Elements/eye.svg" id="eyeicon-login" />
                                        </span>
                                    </div>
                                </div>
                            </div>
                            <div class="modal-footer d-flex justify-content-center" style="margin-top:10px">
                                <div class="d-flex justify-content-center">
                                    <div class="d-flex justify-content-center col-5 mb-2 ">
                                        <button class="btn btn-outline-secondary rounded-0" aria-pressed="true" id="addmodalbtn">
                                            ADD +
                                        </button>
                                    </div>
                                </div>
                            </div>
                            <!--SAVE CHANGES FOR MODAL PROMPT-->
                            <template id="my-template">
                                <swal-title>
                                    Save changes to "Untitled 1" before closing?
                                </swal-title>
                                <swal-icon type="warning" color="red"></swal-icon>
                                <swal-button type="confirm">
                                    Save As
                                </swal-button>
                                <swal-button type="cancel">
                                    Cancel
                                </swal-button>
                                <swal-button type="deny">
                                    Close without Saving
                                </swal-button>
                                <swal-param name="allowEscapeKey" value="false" />
                                <swal-param name="customClass"
                                            value='{ "popup": "my-popup" }' />
                                <swal-function-param name="didOpen"
                                                     value="popup => console.log(popup)" />
                            </template>
                        }
                        </div>
                </div>
            </div>
        </div>
        <!--ADMIN DASHBOARD-->
        <section class="text-light p-5">
            <div class="container d-flex justify-content-center" id="maincontainer">
                <div class="justify-content-center" id="content">
                    <!--Title-->
                    <div class="d-flex justify-content-center col-12" id="title" style="display:block; white-space:nowrap; margin-left:auto; margin-right:auto;">
                        <h2 align="center">
                            / ADMIN DASHBOARD /
                        </h2>
                    </div>
                    <!--TABLE-->
                    <div class="d-flex justify-content-center w-100" style="margin-top:20px">
                        <div class="row w-100" id="passrepotable">
                            <!--ITEMS-->
                            <div class="col-4" id="table-leftcolumn">
                                <!--Search bar-->
                                <div class="d-flex justify-content-center">
                                    <a style="width:95%">
                                        <div class="input-group" style="margin-top:10px;">
                                            <span class="input-group-text rounded-0" id="basic-addon1"><img id="searchicon" src="~/Elements/searchicon.png" /></span>
                                            <input type="text" class="form-control form-control-search rounded-0" placeholder="SEARCH" aria-label="Username" aria-describedby="basic-addon1" id="searchform">
                                        </div>
                                    </a>
                                </div>

                                <!-- Button trigger modal -->
                                <div class="d-flex justify-content-center">
                                    <button type="button" class="btn btn-outline-secondary rounded-0" data-bs-toggle="modal" data-bs-target="#addbutton-modal" id="additembtn" style="margin-top:10px;">
                                        ADD ITEM +
                                    </button>
                                </div>

                                <div class="list-group rounded-0 overflow-auto flipped scroller" id="list-tab" role="tablist" style="margin-top:10px; height:500px;">
                                    @{foreach (var UserEntry in Model.UserCredList.Where(x => x.EXPIRY_DATE > DateTime.Now || x.EXPIRY_DATE == null))
                                        {
                                            <a class="list-group-item list-group-item-action list-group-item-1" id="list-@UserEntry.ID-list" data-bs-toggle="list" href="#list-@UserEntry.ID" role="tab" aria-controls="list-@UserEntry.ID">@UserEntry.USERNAME</a>
                                        }
                                    }
                                </div>
                                <script>
                                    $(document).ready(function () {
                                        //SEARCH FUNCTION
                                        $("#searchform").on("keyup", function () {
                                            var value = $(this).val().toLowerCase();
                                            $(".list-group-item-1").filter(function () {
                                                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1);
                                            });
                                        });

                                        //SHOW INITIAL DISPLAY
                                        checkListVisibility(); // Call the function on page load

                                        function checkListVisibility() {
                                            var listItems = $(".list-group-item-1");
                                            var listWelcome = $("#list-welcome-1");
                                            var deletebtn = document.getElementById('deleteallbtn')

                                            if (listItems.length > 0) {
                                                listWelcome.hide();
                                                deletebtn.classList.remove('disabled');
                                            } else {
                                                listWelcome.show();
                                                deletebtn.classList.add('disabled');
                                            }
                                        }
                                    });
                                </script>
                            </div>
                            <!--ITEM DETAILS-->
                            <div class="col-8" id="table-rightcolumn">
                                <div class="tab-content" id="nav-tabContent">
                                    <!--INITIAL DISPLAY: DISPLAY IF NO ITEMS-->
                                    <div class="show" id="list-welcome-1">
                                        <div class="container w-100">
                                            <!--DISPLAY DETAILS-->
                                            <div class="item-table w-100 table-form-title" style="margin-top:270px">
                                                <div class="tbl-details d-flex justify-content-center">
                                                    <h2 class="form-control-title" style="text-align:center; width:900px" id="Title-welcome">
                                                        ADD AN ITEM TO START
                                                        <p style="font-size:14px; color:gray">Click the add item button to save your details.</p>
                                                    </h2>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    @{foreach (var UserEntry in Model.UserCredList.Where(x => x.EXPIRY_DATE > DateTime.Now || x.EXPIRY_DATE == null))
                                        {
                                            var UserDetails = Model.UserDetailsList.Where(x => x.UID.Equals(UserEntry.ID)).FirstOrDefault();
                                            //using (@Html.BeginForm("Index", "Login", FormMethod.Post)) { };
                                            var decryptedPassword = Encrypter.DecryptString(UserEntry.PASSWORD);
                                            var decryptedPIN = Encrypter.DecryptString(UserDetails.PIN);

                                            //PER ITEM DETAILS
                                            <div class="tab-pane show" id="list-@UserEntry.ID" role="tabpanel" aria-labelledby="list-@UserEntry.ID-list">
                                                <div class="container">
                                                    <!--DETAILS-->
                                                    <div class="mt-3 item-table w-100 table-form-title" style="margin-top:-30px">
                                                        <!--TITLE NAME-->
                                                        <div class="tbl-details">
                                                            <h2>
                                                                <input type="text" minlength="1" maxlength="20" readonly style="width:100%" class="form-control-title" id="Title-@UserEntry.ID.ToString()" value="@UserEntry.USERNAME" spellcheck="false" placeholder="Title">
                                                            </h2>
                                                        </div>
                                                        <!--FIRSTNAME INPUT-->
                                                        <div class="mt-3 tbl-details" id="tbl-Lname-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">First Name</p>
                                                                <input type="text" maxlength="50" readonly class="form-control-plaintext" style="width: 100%; text-transform: capitalize;" id="Fname-@UserEntry.ID.ToString()" value="@UserDetails.FIRSTNAME" spellcheck="false">
                                                                <span>
                                                                    <!--CC icon-->
                                                                    <img src="~/Elements/copytoclipboard.svg" class="ccicon-@UserEntry.ID" id="cciconFname-@UserEntry.ID" />
                                                                </span>
                                                                <script type="text/javascript">
                                                                //disappear if input is null
                                                                window.addEventListener('load', function () {
                                                                    checkInputFname();
                                                                });

                                                                function checkInputFName() {
                                                                    const inputElement = document.getElementById('Fname-@UserEntry.ID.ToString()');
                                                                    const tblDetail = document.getElementById('tbl-Fname-@UserEntry.ID.ToString()')

                                                                    // Check if the value is null or empty
                                                                    if (!inputElement.value.trim()) {
                                                                        tblDetail.style.display = 'none'; // Hide the input element
                                                                    } else {
                                                                        tblDetail.style.display = 'block'; // Show the input element
                                                                    }
                                                                }

                                                                //copy to clipboard function
                                                                function initializeCCIcon() {
                                                                    let ccicon = document.getElementById('cciconFname-@UserEntry.ID.ToString()');
                                                                    var formdisplay = document.getElementById('Fname-@UserEntry.ID.ToString()');

                                                                    // Make sure the elements are not null before accessing their properties
                                                                    if (ccicon && formdisplay) {

                                                                        ccicon.style.display = "none";

                                                                        formdisplay.addEventListener("mouseenter", function () {
                                                                            ccicon.style.display = "block";
                                                                            formdisplay.classlist.add("hover");
                                                                            //ccicon.classList.add = "hidden";

                                                                        });

                                                                        formdisplay.addEventListener("mouseleave", function () {
                                                                        const isTargetIcon = event.relatedTarget === ccicon || ccicon.contains(event.relatedTarget);
                                                                            if (!isTargetIcon) {
                                                                                ccicon.style.display = "none";
                                                                            }
                                                                        });

                                                                        ccicon.addEventListener("mouseenter", function () {
                                                                            formdisplay.classList.add("hover");
                                                                        });

                                                                        ccicon.addEventListener("mousedown", function (event) {
                                                                            event.preventDefault(); // prevent default action to avoid losing focus
                                                                            togglecc_table();
                                                                        });

                                                                        function togglecc_table() {
                                                                            var copyText = formdisplay

                                                                            // Copy the text inside the text field
                                                                            navigator.clipboard.writeText(copyText.value);

                                                                            // Alert the copied text
                                                                            /*alert("Copied the text: " + copyText.value);*/
                                                                            const Toast = Swal.mixin({
                                                                                toast: true,
                                                                                position: 'top',
                                                                                showConfirmButton: false,
                                                                                timer: 3000,
                                                                                timerProgressBar: true,
                                                                                didOpen: (toast) => {
                                                                                    toast.addEventListener('mouseenter', Swal.stopTimer)
                                                                                    toast.addEventListener('mouseleave', Swal.resumeTimer)
                                                                                }
                                                                            })

                                                                            Toast.fire({
                                                                                icon: 'success',
                                                                                iconColor: '#70cc72',
                                                                                title: 'Copied "First name"!',
                                                                                background: '#1a1a1a',
                                                                            })
                                                                            ccicon.style.display = "none";
                                                                        }
                                                                    }
                                                                }
                                                                  // Call the function after the document has finished loading
                                                                  window.addEventListener('load', initializeCCIcon);
                                                                </script>
                                                            </div>
                                                        </div>
                                                        <!--NAME INPUT-->
                                                        <div class="mt-3 tbl-details" id="tbl-Lname-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">Last Name</p>
                                                                <input type="text" maxlength="50" readonly class="form-control-plaintext" style="width: 100%; text-transform: capitalize;" id="Lname-@UserEntry.ID.ToString()" value="@UserDetails.LASTNAME" spellcheck="false">
                                                                <span>
                                                                    <!--CC icon-->
                                                                    <img src="~/Elements/copytoclipboard.svg" class="ccicon-@UserEntry.ID" id="cciconLname-@UserEntry.ID" />
                                                                </span>
                                                                <script type="text/javascript">
                                                                //disappear if input is null
                                                                window.addEventListener('load', function () {
                                                                    checkInputLname();
                                                                });

                                                                function checkInputLname() {
                                                                    const inputElement = document.getElementById('Lname-@UserEntry.ID.ToString()');
                                                                    const tblDetail = document.getElementById('tbl-Lname-@UserEntry.ID.ToString()')

                                                                    // Check if the value is null or empty
                                                                    if (!inputElement.value.trim()) {
                                                                        tblDetail.style.display = 'none'; // Hide the input element
                                                                    } else {
                                                                        tblDetail.style.display = 'block'; // Show the input element
                                                                    }
                                                                }

                                                                //copy to clipboard function
                                                                function initializeCCIcon() {
                                                                    let ccicon = document.getElementById('cciconLname-@UserEntry.ID.ToString()');
                                                                    var formdisplay = document.getElementById('Lname-@UserEntry.ID.ToString()');

                                                                    // Make sure the elements are not null before accessing their properties
                                                                    if (ccicon && formdisplay) {

                                                                        ccicon.style.display = "none";

                                                                        formdisplay.addEventListener("mouseenter", function () {
                                                                            ccicon.style.display = "block";
                                                                            formdisplay.classlist.add("hover");
                                                                            //ccicon.classList.add = "hidden";

                                                                        });

                                                                        formdisplay.addEventListener("mouseleave", function () {
                                                                        const isTargetIcon = event.relatedTarget === ccicon || ccicon.contains(event.relatedTarget);
                                                                            if (!isTargetIcon) {
                                                                                ccicon.style.display = "none";
                                                                            }
                                                                        });

                                                                        ccicon.addEventListener("mouseenter", function () {
                                                                            formdisplay.classList.add("hover");
                                                                        });

                                                                        ccicon.addEventListener("mousedown", function (event) {
                                                                            event.preventDefault(); // prevent default action to avoid losing focus
                                                                            togglecc_table();
                                                                        });

                                                                        function togglecc_table() {
                                                                            var copyText = formdisplay

                                                                            // Copy the text inside the text field
                                                                            navigator.clipboard.writeText(copyText.value);

                                                                            // Alert the copied text
                                                                            /*alert("Copied the text: " + copyText.value);*/
                                                                            const Toast = Swal.mixin({
                                                                                toast: true,
                                                                                position: 'top',
                                                                                showConfirmButton: false,
                                                                                timer: 3000,
                                                                                timerProgressBar: true,
                                                                                didOpen: (toast) => {
                                                                                    toast.addEventListener('mouseenter', Swal.stopTimer)
                                                                                    toast.addEventListener('mouseleave', Swal.resumeTimer)
                                                                                }
                                                                            })

                                                                            Toast.fire({
                                                                                icon: 'success',
                                                                                iconColor: '#70cc72',
                                                                                title: 'Copied "Last name"!',
                                                                                background: '#1a1a1a',
                                                                            })
                                                                            ccicon.style.display = "none";
                                                                        }
                                                                    }
                                                                }
                                                                  // Call the function after the document has finished loading
                                                                  window.addEventListener('load', initializeCCIcon);
                                                                </script>
                                                            </div>
                                                        </div>
                                                        <!--USERNAME INPUT-->
                                                        <div class="mt-3 tbl-details" id="tbl-username-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">Username</p>
                                                                <input type="text" maxlength="50" readonly class="form-control-plaintext readonly" style="width:100%" id="Username-@UserEntry.ID.ToString()" value="@UserEntry.USERNAME" spellcheck="false">
                                                                <span>
                                                                    <!--CC icon-->
                                                                    <img src="~/Elements/copytoclipboard.svg" class="ccicon-@UserEntry.ID" id="cciconUsername-@UserEntry.ID.ToString()" />
                                                                </span>
                                                                <script type="text/javascript">
                                                                //disappear if input is null
                                                                window.addEventListener('load', function () {
                                                                    checkInputusername();
                                                                });

                                                                function checkInputusername() {
                                                                const inputElement = document.getElementById('Username-@UserEntry.ID.ToString()');
                                                                const tblDetail = document.getElementById('tbl-username-@UserEntry.ID.ToString()')

                                                                // Check if the value is null or empty
                                                                    if (!inputElement.value.trim()) {
                                                                        tblDetail.style.display = 'none'; // Hide the input element
                                                                    } else {
                                                                        tblDetail.style.display = 'block'; // Show the input element
                                                                    }
                                                                }

                                                                //copy to clipboard function
                                                                function initializeCCIcon() {
                                                                    let ccicon = document.getElementById('cciconUsername-@UserEntry.ID.ToString()');
                                                                    var formdisplay = document.getElementById('Username-@UserEntry.ID.ToString()');

                                                                    // Make sure the elements are not null before accessing their properties
                                                                    if (ccicon && formdisplay) {

                                                                        ccicon.style.display = "none";

                                                                        formdisplay.addEventListener("mouseenter", function () {
                                                                            ccicon.style.display = "block";
                                                                            formdisplay.classlist.add("hover");
                                                                            //ccicon.classList.add = "hidden";

                                                                        });

                                                                        formdisplay.addEventListener("mouseleave", function () {
                                                                            const isTargetIcon = event.relatedTarget === ccicon || ccicon.contains(event.relatedTarget);
                                                                            if (!isTargetIcon) {
                                                                                ccicon.style.display = "none";
                                                                            }
                                                                        });

                                                                        ccicon.addEventListener("mouseenter", function () {
                                                                            formdisplay.classList.add("hover");
                                                                        });

                                                                        ccicon.addEventListener("mousedown", function (event) {
                                                                            event.preventDefault(); // prevent default action to avoid losing focus
                                                                            togglecc_table();
                                                                        });

                                                                        function togglecc_table() {
                                                                            var copyText = formdisplay

                                                                        // Copy the text inside the text field
                                                                        navigator.clipboard.writeText(copyText.value);

                                                                        // Alert the copied text
                                                                        /*alert("Copied the text: " + copyText.value);*/
                                                                        const Toast = Swal.mixin({
                                                                            toast: true,
                                                                            position: 'top',
                                                                            showConfirmButton: false,
                                                                            timer: 3000,
                                                                            timerProgressBar: true,
                                                                            didOpen: (toast) => {
                                                                                toast.addEventListener('mouseenter', Swal.stopTimer)
                                                                                toast.addEventListener('mouseleave', Swal.resumeTimer)
                                                                            }
                                                                        })

                                                                        Toast.fire({
                                                                            icon: 'success',
                                                                            iconColor: '#70cc72',
                                                                            title: 'Copied "Username"!',
                                                                            background: '#1a1a1a',
                                                                        })
                                                                        ccicon.style.display = "none";
                                                                    }
                                                                }
                                                            }
                                                                // Call the function after the document has finished loading
                                                                window.addEventListener('load', initializeCCIcon);
                                                                </script>
                                                            </div>
                                                        </div>
                                                        <!--EMAIL INPUT-->
                                                        <div class="mt-3 tbl-details" id="tbl-email-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">Email</p>
                                                                <input type="email" maxlength="50" readonly class="form-control-plaintext" style="width:100%" id="Email-@UserEntry.ID.ToString()" value="@UserEntry.EMAIL" spellcheck="false">
                                                                <span>
                                                                    <!--CC icon-->
                                                                    <img src="~/Elements/copytoclipboard.svg" class="ccicon-@UserEntry.ID.ToString()" id="cciconEmail-@UserEntry.ID.ToString()" />
                                                                </span>
                                                                <script type="text/javascript">
                                                                //disappear if input is null
                                                                window.addEventListener('load', function () {
                                                                    checkInputemail();
                                                                });

                                                                function checkInputemail() {
                                                                    const inputElement = document.getElementById('Email-@UserEntry.ID.ToString()');
                                                                    const tblDetail = document.getElementById('tbl-email-@UserEntry.ID.ToString()')

                                                                    // Check if the value is null or empty
                                                                    if (!inputElement.value.trim()) {
                                                                        tblDetail.style.display = 'none'; // Hide the input element
                                                                    } else {
                                                                        tblDetail.style.display = 'block'; // Show the input element
                                                                    }
                                                                }

                                                                //copy to clipboard function
                                                                function initializeCCIcon() {
                                                                    let ccicon = document.getElementById('cciconEmail-@UserEntry.ID.ToString()');
                                                                    var formdisplay = document.getElementById('Email-@UserEntry.ID.ToString()');

                                                                    // Make sure the elements are not null before accessing their properties
                                                                    if (ccicon && formdisplay) {

                                                                        ccicon.style.display = "none";

                                                                        formdisplay.addEventListener("mouseenter", function () {
                                                                            ccicon.style.display = "block";
                                                                            formdisplay.classlist.add("hover");
                                                                            //ccicon.classList.add = "hidden";

                                                                        });

                                                                        formdisplay.addEventListener("mouseleave", function () {
                                                                        const isTargetIcon = event.relatedTarget === ccicon || ccicon.contains(event.relatedTarget);
                                                                            if (!isTargetIcon) {
                                                                                ccicon.style.display = "none";
                                                                            }
                                                                        });

                                                                        ccicon.addEventListener("mouseenter", function () {
                                                                            formdisplay.classList.add("hover");
                                                                        });

                                                                        ccicon.addEventListener("mousedown", function (event) {
                                                                            event.preventDefault(); // prevent default action to avoid losing focus
                                                                            togglecc_table();
                                                                        });

                                                                         function togglecc_table() {
                                                                            var copyText = formdisplay

                                                                            // Copy the text inside the text field
                                                                            navigator.clipboard.writeText(copyText.value);

                                                                             // Alert the copied text
                                                                             /*alert("Copied the text: " + copyText.value);*/
                                                                             const Toast = Swal.mixin({
                                                                                 toast: true,
                                                                                 position: 'top',
                                                                                 showConfirmButton: false,
                                                                                 timer: 3000,
                                                                                 timerProgressBar: true,
                                                                                 didOpen: (toast) => {
                                                                                     toast.addEventListener('mouseenter', Swal.stopTimer)
                                                                                     toast.addEventListener('mouseleave', Swal.resumeTimer)
                                                                                 }
                                                                             })

                                                                             Toast.fire({
                                                                                 icon: 'success',
                                                                                 iconColor: '#70cc72',
                                                                                 title: 'Copied "Email"!',
                                                                                 background: '#1a1a1a',
                                                                             })
                                                                             ccicon.style.display = "none";
                                                                        }
                                                                    }
                                                                }
                                                                  // Call the function after the document has finished loading
                                                                  window.addEventListener('load', initializeCCIcon);
                                                                </script>
                                                            </div>
                                                        </div>
                                                        <!--PASSWORD INPUT-->
                                                        <div class="mt-3 tbl-details" id="tbl-password-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">Password</p>
                                                                <input type="password" maxlength="50" readonly class="form-control-plaintext" style="width:100%" id="Password-@UserEntry.ID.ToString()" value="@decryptedPassword" spellcheck="false">
                                                                <span>
                                                                    <!--Eye and CC icon-->
                                                                    <img src="~/Elements/eye-white.svg" class="eyeiconpassword-@UserEntry.ID" id="eyeiconPassword-@UserEntry.ID" />
                                                                    <img src="~/Elements/copytoclipboard.svg" class="cciconpassword-@UserEntry.ID" id="cciconPassword-@UserEntry.ID" />
                                                                </span>
                                                                <script type="text/javascript">
                                                                //disappear if input is null
                                                                window.addEventListener('load', function () {
                                                                    checkInputpassword();
                                                                });

                                                                function checkInputpassword() {
                                                                    const inputElement = document.getElementById('Password-@UserEntry.ID.ToString()');
                                                                    const tblDetail = document.getElementById('tbl-password-@UserEntry.ID.ToString()')

                                                                    // Check if the value is null or empty
                                                                    if (!inputElement.value.trim()) {
                                                                        tblDetail.style.display = 'none'; // Hide the input element
                                                                    } else {
                                                                        tblDetail.style.display = 'block'; // Show the input element
                                                                    }
                                                                }
                                                                //copy to clipboard and show password function
                                                                function initializeCCIcon() {
                                                                    let eyeicontbl = document.getElementById('eyeiconPassword-@UserEntry.ID.ToString()');
                                                                    let ccicon = document.getElementById('cciconPassword-@UserEntry.ID.ToString()');
                                                                    var formdisplay = document.getElementById('Password-@UserEntry.ID.ToString()');

                                                                    // Make sure the elements are not null before accessing their properties
                                                                    if (ccicon && formdisplay && eyeicontbl) {

                                                                    eyeicontbl.style.display = "block";
                                                                    ccicon.style.display = "none";

                                                                    formdisplay.addEventListener("mouseenter", function () {
                                                                        ccicon.style.display = "block";
                                                                        formdisplay.classList.add("hover");
                                                                    });

                                                                    formdisplay.addEventListener("mouseleave", function () {
                                                                        const isTargetIcon = event.relatedTarget === ccicon || ccicon.contains(event.relatedTarget);

                                                                        if (!isTargetIcon) {
                                                                            ccicon.style.display = "none";
                                                                        }
                                                                    });

                                                                    eyeicontbl.addEventListener("mouseenter", function () {
                                                                        formdisplay.classList.add("hover");
                                                                    });

                                                                    ccicon.addEventListener("mouseenter", function () {
                                                                        formdisplay.classList.add("hover");
                                                                    });

                                                                    eyeicontbl.addEventListener("mousedown", function (event) {
                                                                        event.preventDefault(); // prevent default action to avoid losing focus
                                                                        toggleshowPW_table();
                                                                    });

                                                                    ccicon.addEventListener("mousedown", function (event) {
                                                                        event.preventDefault(); // prevent default action to avoid losing focus
                                                                        togglecc_table();
                                                                    });

                                                                    function togglecc_table() {
                                                                        var copyText = formdisplay

                                                                        // Copy the text inside the text field
                                                                        navigator.clipboard.writeText(copyText.value);

                                                                        // Alert the copied text
                                                                        /*alert("Copied the text: " + copyText.value);*/
                                                                        const Toast = Swal.mixin({
                                                                            toast: true,
                                                                            position: 'top',
                                                                            showConfirmButton: false,
                                                                            timer: 3000,
                                                                            timerProgressBar: true,
                                                                            didOpen: (toast) => {
                                                                                toast.addEventListener('mouseenter', Swal.stopTimer)
                                                                                toast.addEventListener('mouseleave', Swal.resumeTimer)
                                                                            }
                                                                        })

                                                                        Toast.fire({
                                                                            icon: 'success',
                                                                            iconColor: '#70cc72',
                                                                            title: 'Copied "Password"!',
                                                                            background: '#1a1a1a',
                                                                        })
                                                                        ccicon.style.display = "none";
                                                                    }

                                                                    function toggleshowPW_table() {
                                                                        var show = document.getElementById('Password-@UserEntry.ID.ToString()');
                                                                        if (show.type == 'password') {
                                                                            show.type = 'text';
                                                                            eyeicontbl.src = "/Elements/eye-slash-white.svg"
                                                                        }
                                                                        else {
                                                                            show.type = 'password'
                                                                            eyeicontbl.src = "/Elements/eye-white.svg"

                                                                        }
                                                                        show.focus();
                                                                        }
                                                                    }

                                                                }
                                                                // Call the function after the document has finished loading
                                                                window.addEventListener('load', initializeCCIcon);
                                                                </script>
                                                            </div>
                                                        </div>
                                                        <!--PIN INPUT-->
                                                        <div class="mt-3 tbl-details" id="tbl-PIN-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">PIN</p>
                                                                <input type="password" maxlength="50" readonly class="form-control-plaintext" style="width:100%" id="PIN-@UserEntry.ID.ToString()" value="@decryptedPIN" spellcheck="false">
                                                                <span>
                                                                    <!--Eye and CC icon-->
                                                                    <img src="~/Elements/eye-white.svg" class="eyeiconPIN-@UserEntry.ID" id="eyeiconPIN-@UserEntry.ID" />
                                                                    <img src="~/Elements/copytoclipboard.svg" class="cciconPIN-@UserEntry.ID" id="cciconPIN-@UserEntry.ID" />
                                                                </span>
                                                                <script type="text/javascript">
                                                                //disappear if input is null
                                                                window.addEventListener('load', function () {
                                                                    checkInputPIN();
                                                                });

                                                                function checkInputPIN() {
                                                                    const inputElement = document.getElementById('PIN-@UserEntry.ID.ToString()');
                                                                    const tblDetail = document.getElementById('tbl-PIN-@UserEntry.ID.ToString()')

                                                                    // Check if the value is null or empty
                                                                    if (!inputElement.value.trim()) {
                                                                        tblDetail.style.display = 'none'; // Hide the input element
                                                                    } else {
                                                                        tblDetail.style.display = 'block'; // Show the input element
                                                                    }
                                                                }
                                                                //copy to clipboard and show PIN function
                                                                function initializeCCIcon() {
                                                                    let eyeicontbl = document.getElementById('eyeiconPIN-@UserEntry.ID.ToString()');
                                                                    let ccicon = document.getElementById('cciconPIN-@UserEntry.ID.ToString()');
                                                                    var formdisplay = document.getElementById('PIN-@UserEntry.ID.ToString()');

                                                                    // Make sure the elements are not null before accessing their properties
                                                                    if (ccicon && formdisplay && eyeicontbl) {

                                                                    eyeicontbl.style.display = "block";
                                                                    ccicon.style.display = "none";

                                                                    formdisplay.addEventListener("mouseenter", function () {
                                                                        ccicon.style.display = "block";
                                                                        formdisplay.classList.add("hover");
                                                                    });

                                                                    formdisplay.addEventListener("mouseleave", function () {
                                                                        const isTargetIcon = event.relatedTarget === ccicon || ccicon.contains(event.relatedTarget);

                                                                        if (!isTargetIcon) {
                                                                            ccicon.style.display = "none";
                                                                        }
                                                                    });

                                                                    eyeicontbl.addEventListener("mouseenter", function () {
                                                                        formdisplay.classList.add("hover");
                                                                    });

                                                                    ccicon.addEventListener("mouseenter", function () {
                                                                        formdisplay.classList.add("hover");
                                                                    });

                                                                    eyeicontbl.addEventListener("mousedown", function (event) {
                                                                        event.preventDefault(); // prevent default action to avoid losing focus
                                                                        toggleshowPW_table();
                                                                    });

                                                                    ccicon.addEventListener("mousedown", function (event) {
                                                                        event.preventDefault(); // prevent default action to avoid losing focus
                                                                        togglecc_table();
                                                                    });

                                                                    function togglecc_table() {
                                                                        var copyText = formdisplay

                                                                        // Copy the text inside the text field
                                                                        navigator.clipboard.writeText(copyText.value);

                                                                        // Alert the copied text
                                                                        /*alert("Copied the text: " + copyText.value);*/
                                                                        const Toast = Swal.mixin({
                                                                            toast: true,
                                                                            position: 'top',
                                                                            showConfirmButton: false,
                                                                            timer: 3000,
                                                                            timerProgressBar: true,
                                                                            didOpen: (toast) => {
                                                                                toast.addEventListener('mouseenter', Swal.stopTimer)
                                                                                toast.addEventListener('mouseleave', Swal.resumeTimer)
                                                                            }
                                                                        })

                                                                        Toast.fire({
                                                                            icon: 'success',
                                                                            iconColor: '#70cc72',
                                                                            title: 'Copied "PIN"!',
                                                                            background: '#1a1a1a',
                                                                        })
                                                                        ccicon.style.display = "none";
                                                                    }

                                                                    function toggleshowPW_table() {
                                                                        var show = document.getElementById('PIN-@UserEntry.ID.ToString()');
                                                                        if (show.type == 'password') {
                                                                            show.type = 'text';
                                                                            eyeicontbl.src = "/Elements/eye-slash-white.svg"
                                                                        }
                                                                        else {
                                                                            show.type = 'password'
                                                                            eyeicontbl.src = "/Elements/eye-white.svg"

                                                                        }
                                                                        show.focus();
                                                                        }
                                                                    }

                                                                }
                                                                // Call the function after the document has finished loading
                                                                window.addEventListener('load', initializeCCIcon);
                                                                </script>
                                                            </div>
                                                        </div>
                                                        <!--TIMEOUT INPUT-->
                                                        <div class="mt-3 tbl-details" id="tbl-TimeOut-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">ICELock Timeout</p>
                                                                <input type="text" maxlength="2" readonly class="form-control-plaintext readonly" style="width:100%" id="Timeout-@UserEntry.ID.ToString()" value="@UserDetails.TIMEOUT" spellcheck="false">
                                                            </div>
                                                        </div>
                                                    </div>
                                                    <!--DATE INPUT-->
                                                    <div class="col-12 d-flex justify-content-center" style="display: none;">
                                                        <div style="color: gray; margin-right: 60px; margin-bottom: 60px; margin-top: 30px;" id="dateinfo-@UserEntry.ID">
                                                            <p>Created on @UserEntry.DATE_CREATED</p>
                                                        </div>
                                                    </div>
                                                    <!--HIDDEN CONFIRM AND CANCEL BTNS-->
                                                    <div class="col-11 d-flex justify-content-center">
                                                        <div class="mt-4 mb-4" style="color:gray; display:none;" id="editbtns-@UserEntry.ID">
                                                            <button class="btn btn-outline-secondary rounded-0" onclick="updateEntry('@UserEntry.ID', '@UserEntry.ID.ToString()', '@UserEntry.USERNAME')" style="margin-right:8px;" aria-pressed="true" id="editacceptbtn">
                                                                CONFIRM
                                                            </button>
                                                            <button class="btn btn-outline-secondary rounded-0" onclick="cancelEdit()" aria-pressed="true" id="editcancelbtn">
                                                                CANCEL
                                                            </button>
                                                        </div>
                                                    </div>
                                                </div>
                                                <nav class="menu" id="edit-menu-@UserEntry.ID">
                                                    <input type="checkbox" href="#" class="menu-open" name="menu-open" id="menu-open-@UserEntry.ID.ToString()" />
                                                    <label class="menu-open-button" for="menu-open-@UserEntry.ID.ToString()">
                                                        <span class="lines line-1"></span>
                                                        <span class="lines line-2"></span>
                                                        <span class="lines line-3"></span>
                                                    </label>
                                                    <a class="menu-item red" onclick="deleteUser('@UserEntry.ID','@UserEntry.USERNAME')"> <i class="fa fa-trash"></i> </a>
                                                    <a class="menu-item green" onclick="toggleEdit()"> <i class="fa fa-pen"></i> </a>
                                                </nav>
                                            </div>
                                        }
                                    }
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        <!--ADMIN TRASHBOARD-->
        <section class="text-light p-5">
            <div class="container d-flex justify-content-center" id="maincontainer">
                <div class="justify-content-center" id="content">
                    <!--Title-->
                    <div class="d-flex justify-content-center col-12" id="title" style="display:block; white-space:nowrap; margin-left:auto; margin-right:auto;">
                        <h2 align="center">
                            / ADMIN TRASHBOARD /
                        </h2>
                    </div>
                    <!--TABLE-->
                    <div class="d-flex justify-content-center w-100" style="margin-top:20px">
                        <div class="row w-100" id="passrepotable">
                            <!--ITEMS-->
                            <div class="col-4" id="table-leftcolumn">
                                <!--Search bar-->
                                <div class="d-flex justify-content-center">
                                    <a style="width:95%">
                                        <div class="input-group" style="margin-top:10px;">
                                            <span class="input-group-text rounded-0" id="basic-addon1"><img id="searchicon" src="~/Elements/searchicon.png" /></span>
                                            <input type="text" class="form-control form-control-search rounded-0" placeholder="SEARCH" aria-label="Username" aria-describedby="basic-addon1" id="searchform">
                                        </div>
                                    </a>
                                </div>

                                <div class="list-group rounded-0 overflow-auto flipped scroller" id="list-tab" role="tablist" style="margin-top:10px; height:500px;">
                                    @{foreach (var UserEntry in Model.UserCredList.Where(x => x.STATUS.Equals(false) && x.EXPIRY_DATE <= DateTime.Now))
                                        {
                                            <a class="list-group-item list-group-item-action list-group-item-0" id="list-@UserEntry.ID-list" data-bs-toggle="list" href="#list-@UserEntry.ID" role="tab" aria-controls="list-@UserEntry.ID">@UserEntry.USERNAME</a>
                                        }
                                    }
                                </div>
                                <script>
                                    $(document).ready(function () {
                                        //SEARCH FUNCTION
                                        $("#searchform").on("keyup", function () {
                                            var value = $(this).val().toLowerCase();
                                            $(".list-group-item-0").filter(function () {
                                                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1);
                                            });
                                        });

                                        //SHOW INITIAL DISPLAY
                                        checkListVisibility(); // Call the function on page load

                                        function checkListVisibility() {
                                            var listItems = $(".list-group-item-0");
                                            var listWelcome = $("#list-welcome-0");
                                            var deletebtn = document.getElementById('deleteallbtn')

                                            if (listItems.length > 0) {
                                                listWelcome.hide();
                                                deletebtn.classList.remove('disabled');
                                            } else {
                                                listWelcome.show();
                                                deletebtn.classList.add('disabled');
                                            }
                                        }
                                    });
                                </script>
                            </div>
                            <!--ITEM DETAILS-->
                            <div class="col-8" id="table-rightcolumn">
                                <div class="tab-content" id="nav-tabContent">
                                    <!--INITIAL DISPLAY: DISPLAY IF NO ITEMS-->
                                    <div class="show" id="list-welcome-0">
                                        <div class="container w-100">
                                            <!--DISPLAY DETAILS-->
                                            <div class="item-table w-100 table-form-title" style="margin-top:250px">
                                                <div class="tbl-details-0-0 d-flex justify-content-center">
                                                    <h2 class="form-control-title-0" style="text-align:center; width:900px" id="Title-welcome">
                                                        ADD AN ITEM TO START
                                                        <p style="font-size:14px; color:gray">Click the add item button to save your details.</p>
                                                    </h2>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    @{foreach (var UserEntry in Model.UserCredList.Where(x => x.STATUS.Equals(false)  && x.EXPIRY_DATE <= DateTime.Now))
                                        {
                                            var UserDetails = Model.UserDetailsList.Where(x => x.UID.Equals(UserEntry.ID)).FirstOrDefault();
                                            //using (@Html.BeginForm("Index", "Login", FormMethod.Post)) { };
                                            var decryptedPassword = Encrypter.DecryptString(UserEntry.PASSWORD);
                                            var decryptedPIN = Encrypter.DecryptString(UserDetails.PIN);
                                            //PER ITEM DETAILS
                                            <div class="tab-pane show" id="list-@UserEntry.ID" role="tabpanel" aria-labelledby="list-@UserEntry.ID-list">
                                                <div class="container">
                                                    <!--DETAILS-->
                                                    <div class="mt-3 item-table w-100 table-form-title" style="margin-top:-30px">
                                                        <!--TITLE NAME-->
                                                        <div class="tbl-details-0">
                                                            <h2>
                                                                <input type="text" minlength="1" maxlength="20" readonly style="width:100%" class="form-control-title-0" id="Title-@UserEntry.ID.ToString()" value="@UserEntry.USERNAME" spellcheck="false" placeholder="Title">
                                                            </h2>
                                                        </div>
                                                        <!--FIRSTNAME INPUT-->
                                                        <div class="mt-3 tbl-details-0" id="tbl-Lname-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">First Name</p>
                                                                <input type="text" maxlength="50" readonly class="form-control-plaintext" style="width: 100%; text-transform: capitalize;" id="Fname-@UserEntry.ID.ToString()" value="@UserDetails.FIRSTNAME" spellcheck="false">
                                                                <span>
                                                                    <!--CC icon-->
                                                                    <img src="~/Elements/copytoclipboard.svg" class="ccicon-@UserEntry.ID" id="cciconFname-@UserEntry.ID" />
                                                                </span>
                                                                <script type="text/javascript">
                                                                //disappear if input is null
                                                                window.addEventListener('load', function () {
                                                                    checkInputFname();
                                                                });

                                                                function checkInputFName() {
                                                                    const inputElement = document.getElementById('Fname-@UserEntry.ID.ToString()');
                                                                    const tblDetail = document.getElementById('tbl-Fname-@UserEntry.ID.ToString()')

                                                                    // Check if the value is null or empty
                                                                    if (!inputElement.value.trim()) {
                                                                        tblDetail.style.display = 'none'; // Hide the input element
                                                                    } else {
                                                                        tblDetail.style.display = 'block'; // Show the input element
                                                                    }
                                                                }

                                                                //copy to clipboard function
                                                                function initializeCCIcon() {
                                                                    let ccicon = document.getElementById('cciconFname-@UserEntry.ID.ToString()');
                                                                    var formdisplay = document.getElementById('Fname-@UserEntry.ID.ToString()');

                                                                    // Make sure the elements are not null before accessing their properties
                                                                    if (ccicon && formdisplay) {

                                                                        ccicon.style.display = "none";

                                                                        formdisplay.addEventListener("mouseenter", function () {
                                                                            ccicon.style.display = "block";
                                                                            formdisplay.classlist.add("hover");
                                                                            //ccicon.classList.add = "hidden";

                                                                        });

                                                                        formdisplay.addEventListener("mouseleave", function () {
                                                                        const isTargetIcon = event.relatedTarget === ccicon || ccicon.contains(event.relatedTarget);
                                                                            if (!isTargetIcon) {
                                                                                ccicon.style.display = "none";
                                                                            }
                                                                        });

                                                                        ccicon.addEventListener("mouseenter", function () {
                                                                            formdisplay.classList.add("hover");
                                                                        });

                                                                        ccicon.addEventListener("mousedown", function (event) {
                                                                            event.preventDefault(); // prevent default action to avoid losing focus
                                                                            togglecc_table();
                                                                        });

                                                                        function togglecc_table() {
                                                                            var copyText = formdisplay

                                                                            // Copy the text inside the text field
                                                                            navigator.clipboard.writeText(copyText.value);

                                                                            // Alert the copied text
                                                                            /*alert("Copied the text: " + copyText.value);*/
                                                                            const Toast = Swal.mixin({
                                                                                toast: true,
                                                                                position: 'top',
                                                                                showConfirmButton: false,
                                                                                timer: 3000,
                                                                                timerProgressBar: true,
                                                                                didOpen: (toast) => {
                                                                                    toast.addEventListener('mouseenter', Swal.stopTimer)
                                                                                    toast.addEventListener('mouseleave', Swal.resumeTimer)
                                                                                }
                                                                            })

                                                                            Toast.fire({
                                                                                icon: 'success',
                                                                                iconColor: '#70cc72',
                                                                                title: 'Copied "First name"!',
                                                                                background: '#1a1a1a',
                                                                            })
                                                                            ccicon.style.display = "none";
                                                                        }
                                                                    }
                                                                }
                                                                  // Call the function after the document has finished loading
                                                                  window.addEventListener('load', initializeCCIcon);
                                                                </script>
                                                            </div>
                                                        </div>
                                                        <!--LASTNAME INPUT-->
                                                        <div class="mt-3 tbl-details-0" id="tbl-Lname-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">Last Name</p>
                                                                <input type="text" maxlength="50" readonly class="form-control-plaintext" style="width: 100%; text-transform: capitalize;" id="Lname-@UserEntry.ID.ToString()" value="@UserDetails.LASTNAME" spellcheck="false">
                                                                <span>
                                                                    <!--CC icon-->
                                                                    <img src="~/Elements/copytoclipboard.svg" class="ccicon-@UserEntry.ID" id="cciconLname-@UserEntry.ID" />
                                                                </span>
                                                                <script type="text/javascript">
                                                                //disappear if input is null
                                                                window.addEventListener('load', function () {
                                                                    checkInputLname();
                                                                });

                                                                function checkInputLname() {
                                                                    const inputElement = document.getElementById('Lname-@UserEntry.ID.ToString()');
                                                                    const tblDetail = document.getElementById('tbl-Lname-@UserEntry.ID.ToString()')

                                                                    // Check if the value is null or empty
                                                                    if (!inputElement.value.trim()) {
                                                                        tblDetail.style.display = 'none'; // Hide the input element
                                                                    } else {
                                                                        tblDetail.style.display = 'block'; // Show the input element
                                                                    }
                                                                }

                                                                //copy to clipboard function
                                                                function initializeCCIcon() {
                                                                    let ccicon = document.getElementById('cciconLname-@UserEntry.ID.ToString()');
                                                                    var formdisplay = document.getElementById('Lname-@UserEntry.ID.ToString()');

                                                                    // Make sure the elements are not null before accessing their properties
                                                                    if (ccicon && formdisplay) {

                                                                        ccicon.style.display = "none";

                                                                        formdisplay.addEventListener("mouseenter", function () {
                                                                            ccicon.style.display = "block";
                                                                            formdisplay.classlist.add("hover");
                                                                            //ccicon.classList.add = "hidden";

                                                                        });

                                                                        formdisplay.addEventListener("mouseleave", function () {
                                                                        const isTargetIcon = event.relatedTarget === ccicon || ccicon.contains(event.relatedTarget);
                                                                            if (!isTargetIcon) {
                                                                                ccicon.style.display = "none";
                                                                            }
                                                                        });

                                                                        ccicon.addEventListener("mouseenter", function () {
                                                                            formdisplay.classList.add("hover");
                                                                        });

                                                                        ccicon.addEventListener("mousedown", function (event) {
                                                                            event.preventDefault(); // prevent default action to avoid losing focus
                                                                            togglecc_table();
                                                                        });

                                                                        function togglecc_table() {
                                                                            var copyText = formdisplay

                                                                            // Copy the text inside the text field
                                                                            navigator.clipboard.writeText(copyText.value);

                                                                            // Alert the copied text
                                                                            /*alert("Copied the text: " + copyText.value);*/
                                                                            const Toast = Swal.mixin({
                                                                                toast: true,
                                                                                position: 'top',
                                                                                showConfirmButton: false,
                                                                                timer: 3000,
                                                                                timerProgressBar: true,
                                                                                didOpen: (toast) => {
                                                                                    toast.addEventListener('mouseenter', Swal.stopTimer)
                                                                                    toast.addEventListener('mouseleave', Swal.resumeTimer)
                                                                                }
                                                                            })

                                                                            Toast.fire({
                                                                                icon: 'success',
                                                                                iconColor: '#70cc72',
                                                                                title: 'Copied "Last name"!',
                                                                                background: '#1a1a1a',
                                                                            })
                                                                            ccicon.style.display = "none";
                                                                        }
                                                                    }
                                                                }
                                                                  // Call the function after the document has finished loading
                                                                  window.addEventListener('load', initializeCCIcon);
                                                                </script>
                                                            </div>
                                                        </div>
                                                        <!--USERNAME INPUT-->
                                                        <div class="mt-3 tbl-details-0" id="tbl-username-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">Username</p>
                                                                <input type="text" maxlength="50" readonly class="form-control-plaintext readonly" style="width:100%" id="Username-@UserEntry.ID.ToString()" value="@UserEntry.USERNAME" spellcheck="false">
                                                                <span>
                                                                    <!--CC icon-->
                                                                    <img src="~/Elements/copytoclipboard.svg" class="ccicon-@UserEntry.ID" id="cciconUsername-@UserEntry.ID.ToString()" />
                                                                </span>
                                                                <script type="text/javascript">
                                                                //disappear if input is null
                                                                window.addEventListener('load', function () {
                                                                    checkInputusername();
                                                                });

                                                                function checkInputusername() {
                                                                const inputElement = document.getElementById('Username-@UserEntry.ID.ToString()');
                                                                const tblDetail = document.getElementById('tbl-username-@UserEntry.ID.ToString()')

                                                                // Check if the value is null or empty
                                                                    if (!inputElement.value.trim()) {
                                                                        tblDetail.style.display = 'none'; // Hide the input element
                                                                    } else {
                                                                        tblDetail.style.display = 'block'; // Show the input element
                                                                    }
                                                                }

                                                                //copy to clipboard function
                                                                function initializeCCIcon() {
                                                                    let ccicon = document.getElementById('cciconUsername-@UserEntry.ID.ToString()');
                                                                    var formdisplay = document.getElementById('Username-@UserEntry.ID.ToString()');

                                                                    // Make sure the elements are not null before accessing their properties
                                                                    if (ccicon && formdisplay) {

                                                                        ccicon.style.display = "none";

                                                                        formdisplay.addEventListener("mouseenter", function () {
                                                                            ccicon.style.display = "block";
                                                                            formdisplay.classlist.add("hover");
                                                                            //ccicon.classList.add = "hidden";

                                                                        });

                                                                        formdisplay.addEventListener("mouseleave", function () {
                                                                            const isTargetIcon = event.relatedTarget === ccicon || ccicon.contains(event.relatedTarget);
                                                                            if (!isTargetIcon) {
                                                                                ccicon.style.display = "none";
                                                                            }
                                                                        });

                                                                        ccicon.addEventListener("mouseenter", function () {
                                                                            formdisplay.classList.add("hover");
                                                                        });

                                                                        ccicon.addEventListener("mousedown", function (event) {
                                                                            event.preventDefault(); // prevent default action to avoid losing focus
                                                                            togglecc_table();
                                                                        });

                                                                        function togglecc_table() {
                                                                            var copyText = formdisplay

                                                                        // Copy the text inside the text field
                                                                        navigator.clipboard.writeText(copyText.value);

                                                                        // Alert the copied text
                                                                        /*alert("Copied the text: " + copyText.value);*/
                                                                        const Toast = Swal.mixin({
                                                                            toast: true,
                                                                            position: 'top',
                                                                            showConfirmButton: false,
                                                                            timer: 3000,
                                                                            timerProgressBar: true,
                                                                            didOpen: (toast) => {
                                                                                toast.addEventListener('mouseenter', Swal.stopTimer)
                                                                                toast.addEventListener('mouseleave', Swal.resumeTimer)
                                                                            }
                                                                        })

                                                                        Toast.fire({
                                                                            icon: 'success',
                                                                            iconColor: '#70cc72',
                                                                            title: 'Copied "Username"!',
                                                                            background: '#1a1a1a',
                                                                        })
                                                                        ccicon.style.display = "none";
                                                                    }
                                                                }
                                                            }
                                                                // Call the function after the document has finished loading
                                                                window.addEventListener('load', initializeCCIcon);
                                                                </script>
                                                            </div>
                                                        </div>
                                                        <!--EMAIL INPUT-->
                                                        <div class="mt-3 tbl-details-0" id="tbl-email-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">Email</p>
                                                                <input type="email" maxlength="50" readonly class="form-control-plaintext" style="width:100%" id="Email-@UserEntry.ID.ToString()" value="@UserEntry.EMAIL" spellcheck="false">
                                                                <span>
                                                                    <!--CC icon-->
                                                                    <img src="~/Elements/copytoclipboard.svg" class="ccicon-@UserEntry.ID.ToString()" id="cciconEmail-@UserEntry.ID.ToString()" />
                                                                </span>
                                                                <script type="text/javascript">
                                                                //disappear if input is null
                                                                window.addEventListener('load', function () {
                                                                    checkInputemail();
                                                                });

                                                                function checkInputemail() {
                                                                    const inputElement = document.getElementById('Email-@UserEntry.ID.ToString()');
                                                                    const tblDetail = document.getElementById('tbl-email-@UserEntry.ID.ToString()')

                                                                    // Check if the value is null or empty
                                                                    if (!inputElement.value.trim()) {
                                                                        tblDetail.style.display = 'none'; // Hide the input element
                                                                    } else {
                                                                        tblDetail.style.display = 'block'; // Show the input element
                                                                    }
                                                                }

                                                                //copy to clipboard function
                                                                function initializeCCIcon() {
                                                                    let ccicon = document.getElementById('cciconEmail-@UserEntry.ID.ToString()');
                                                                    var formdisplay = document.getElementById('Email-@UserEntry.ID.ToString()');

                                                                    // Make sure the elements are not null before accessing their properties
                                                                    if (ccicon && formdisplay) {

                                                                        ccicon.style.display = "none";

                                                                        formdisplay.addEventListener("mouseenter", function () {
                                                                            ccicon.style.display = "block";
                                                                            formdisplay.classlist.add("hover");
                                                                            //ccicon.classList.add = "hidden";

                                                                        });

                                                                        formdisplay.addEventListener("mouseleave", function () {
                                                                        const isTargetIcon = event.relatedTarget === ccicon || ccicon.contains(event.relatedTarget);
                                                                            if (!isTargetIcon) {
                                                                                ccicon.style.display = "none";
                                                                            }
                                                                        });

                                                                        ccicon.addEventListener("mouseenter", function () {
                                                                            formdisplay.classList.add("hover");
                                                                        });

                                                                        ccicon.addEventListener("mousedown", function (event) {
                                                                            event.preventDefault(); // prevent default action to avoid losing focus
                                                                            togglecc_table();
                                                                        });

                                                                         function togglecc_table() {
                                                                            var copyText = formdisplay

                                                                            // Copy the text inside the text field
                                                                            navigator.clipboard.writeText(copyText.value);

                                                                             // Alert the copied text
                                                                             /*alert("Copied the text: " + copyText.value);*/
                                                                             const Toast = Swal.mixin({
                                                                                 toast: true,
                                                                                 position: 'top',
                                                                                 showConfirmButton: false,
                                                                                 timer: 3000,
                                                                                 timerProgressBar: true,
                                                                                 didOpen: (toast) => {
                                                                                     toast.addEventListener('mouseenter', Swal.stopTimer)
                                                                                     toast.addEventListener('mouseleave', Swal.resumeTimer)
                                                                                 }
                                                                             })

                                                                             Toast.fire({
                                                                                 icon: 'success',
                                                                                 iconColor: '#70cc72',
                                                                                 title: 'Copied "Email"!',
                                                                                 background: '#1a1a1a',
                                                                             })
                                                                             ccicon.style.display = "none";
                                                                        }
                                                                    }
                                                                }
                                                                  // Call the function after the document has finished loading
                                                                  window.addEventListener('load', initializeCCIcon);
                                                                </script>
                                                            </div>
                                                        </div>
                                                        <!--PASSWORD INPUT-->
                                                        <div class="mt-3 tbl-details-0" id="tbl-password-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">Password</p>
                                                                <input type="password" maxlength="50" readonly class="form-control-plaintext" style="width:100%" id="Password-@UserEntry.ID.ToString()" value="@decryptedPassword" spellcheck="false">
                                                                <span>
                                                                    <!--Eye and CC icon-->
                                                                    <img src="~/Elements/eye-white.svg" class="eyeiconpassword-@UserEntry.ID" id="eyeiconPassword-@UserEntry.ID" />
                                                                    <img src="~/Elements/copytoclipboard.svg" class="cciconpassword-@UserEntry.ID" id="cciconPassword-@UserEntry.ID" />
                                                                </span>
                                                                <script type="text/javascript">
                                                                //disappear if input is null
                                                                window.addEventListener('load', function () {
                                                                    checkInputpassword();
                                                                });

                                                                function checkInputpassword() {
                                                                    const inputElement = document.getElementById('Password-@UserEntry.ID.ToString()');
                                                                    const tblDetail = document.getElementById('tbl-password-@UserEntry.ID.ToString()')

                                                                    // Check if the value is null or empty
                                                                    if (!inputElement.value.trim()) {
                                                                        tblDetail.style.display = 'none'; // Hide the input element
                                                                    } else {
                                                                        tblDetail.style.display = 'block'; // Show the input element
                                                                    }
                                                                }
                                                                //copy to clipboard and show password function
                                                                function initializeCCIcon() {
                                                                    let eyeicontbl = document.getElementById('eyeiconPassword-@UserEntry.ID.ToString()');
                                                                    let ccicon = document.getElementById('cciconPassword-@UserEntry.ID.ToString()');
                                                                    var formdisplay = document.getElementById('Password-@UserEntry.ID.ToString()');

                                                                    // Make sure the elements are not null before accessing their properties
                                                                    if (ccicon && formdisplay && eyeicontbl) {

                                                                    eyeicontbl.style.display = "block";
                                                                    ccicon.style.display = "none";

                                                                    formdisplay.addEventListener("mouseenter", function () {
                                                                        ccicon.style.display = "block";
                                                                        formdisplay.classList.add("hover");
                                                                    });

                                                                    formdisplay.addEventListener("mouseleave", function () {
                                                                        const isTargetIcon = event.relatedTarget === ccicon || ccicon.contains(event.relatedTarget);

                                                                        if (!isTargetIcon) {
                                                                            ccicon.style.display = "none";
                                                                        }
                                                                    });

                                                                    eyeicontbl.addEventListener("mouseenter", function () {
                                                                        formdisplay.classList.add("hover");
                                                                    });

                                                                    ccicon.addEventListener("mouseenter", function () {
                                                                        formdisplay.classList.add("hover");
                                                                    });

                                                                    eyeicontbl.addEventListener("mousedown", function (event) {
                                                                        event.preventDefault(); // prevent default action to avoid losing focus
                                                                        toggleshowPW_table();
                                                                    });

                                                                    ccicon.addEventListener("mousedown", function (event) {
                                                                        event.preventDefault(); // prevent default action to avoid losing focus
                                                                        togglecc_table();
                                                                    });

                                                                    function togglecc_table() {
                                                                        var copyText = formdisplay

                                                                        // Copy the text inside the text field
                                                                        navigator.clipboard.writeText(copyText.value);

                                                                        // Alert the copied text
                                                                        /*alert("Copied the text: " + copyText.value);*/
                                                                        const Toast = Swal.mixin({
                                                                            toast: true,
                                                                            position: 'top',
                                                                            showConfirmButton: false,
                                                                            timer: 3000,
                                                                            timerProgressBar: true,
                                                                            didOpen: (toast) => {
                                                                                toast.addEventListener('mouseenter', Swal.stopTimer)
                                                                                toast.addEventListener('mouseleave', Swal.resumeTimer)
                                                                            }
                                                                        })

                                                                        Toast.fire({
                                                                            icon: 'success',
                                                                            iconColor: '#70cc72',
                                                                            title: 'Copied "Password"!',
                                                                            background: '#1a1a1a',
                                                                        })
                                                                        ccicon.style.display = "none";
                                                                    }

                                                                    function toggleshowPW_table() {
                                                                        var show = document.getElementById('Password-@UserEntry.ID.ToString()');
                                                                        if (show.type == 'password') {
                                                                            show.type = 'text';
                                                                            eyeicontbl.src = "/Elements/eye-slash-white.svg"
                                                                        }
                                                                        else {
                                                                            show.type = 'password'
                                                                            eyeicontbl.src = "/Elements/eye-white.svg"

                                                                        }
                                                                        show.focus();
                                                                        }
                                                                    }

                                                                }
                                                                // Call the function after the document has finished loading
                                                                window.addEventListener('load', initializeCCIcon);
                                                                </script>
                                                            </div>
                                                        </div>
                                                        <!--PIN INPUT-->
                                                        <div class="mt-3 tbl-details-0" id="tbl-PIN-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">PIN</p>
                                                                <input type="password" maxlength="50" readonly class="form-control-plaintext" style="width:100%" id="PIN-@UserEntry.ID.ToString()" value="@decryptedPIN" spellcheck="false">
                                                                <span>
                                                                    <!--Eye and CC icon-->
                                                                    <img src="~/Elements/eye-white.svg" class="eyeiconPIN-@UserEntry.ID" id="eyeiconPIN-@UserEntry.ID" />
                                                                    <img src="~/Elements/copytoclipboard.svg" class="cciconPIN-@UserEntry.ID" id="cciconPIN-@UserEntry.ID" />
                                                                </span>
                                                                <script type="text/javascript">
                                                                //disappear if input is null
                                                                window.addEventListener('load', function () {
                                                                    checkInputPIN();
                                                                });

                                                                function checkInputPIN() {
                                                                    const inputElement = document.getElementById('PIN-@UserEntry.ID.ToString()');
                                                                    const tblDetail = document.getElementById('tbl-PIN-@UserEntry.ID.ToString()')

                                                                    // Check if the value is null or empty
                                                                    if (!inputElement.value.trim()) {
                                                                        tblDetail.style.display = 'none'; // Hide the input element
                                                                    } else {
                                                                        tblDetail.style.display = 'block'; // Show the input element
                                                                    }
                                                                }
                                                                //copy to clipboard and show PIN function
                                                                function initializeCCIcon() {
                                                                    let eyeicontbl = document.getElementById('eyeiconPIN-@UserEntry.ID.ToString()');
                                                                    let ccicon = document.getElementById('cciconPIN-@UserEntry.ID.ToString()');
                                                                    var formdisplay = document.getElementById('PIN-@UserEntry.ID.ToString()');

                                                                    // Make sure the elements are not null before accessing their properties
                                                                    if (ccicon && formdisplay && eyeicontbl) {

                                                                    eyeicontbl.style.display = "block";
                                                                    ccicon.style.display = "none";

                                                                    formdisplay.addEventListener("mouseenter", function () {
                                                                        ccicon.style.display = "block";
                                                                        formdisplay.classList.add("hover");
                                                                    });

                                                                    formdisplay.addEventListener("mouseleave", function () {
                                                                        const isTargetIcon = event.relatedTarget === ccicon || ccicon.contains(event.relatedTarget);

                                                                        if (!isTargetIcon) {
                                                                            ccicon.style.display = "none";
                                                                        }
                                                                    });

                                                                    eyeicontbl.addEventListener("mouseenter", function () {
                                                                        formdisplay.classList.add("hover");
                                                                    });

                                                                    ccicon.addEventListener("mouseenter", function () {
                                                                        formdisplay.classList.add("hover");
                                                                    });

                                                                    eyeicontbl.addEventListener("mousedown", function (event) {
                                                                        event.preventDefault(); // prevent default action to avoid losing focus
                                                                        toggleshowPW_table();
                                                                    });

                                                                    ccicon.addEventListener("mousedown", function (event) {
                                                                        event.preventDefault(); // prevent default action to avoid losing focus
                                                                        togglecc_table();
                                                                    });

                                                                    function togglecc_table() {
                                                                        var copyText = formdisplay

                                                                        // Copy the text inside the text field
                                                                        navigator.clipboard.writeText(copyText.value);

                                                                        // Alert the copied text
                                                                        /*alert("Copied the text: " + copyText.value);*/
                                                                        const Toast = Swal.mixin({
                                                                            toast: true,
                                                                            position: 'top',
                                                                            showConfirmButton: false,
                                                                            timer: 3000,
                                                                            timerProgressBar: true,
                                                                            didOpen: (toast) => {
                                                                                toast.addEventListener('mouseenter', Swal.stopTimer)
                                                                                toast.addEventListener('mouseleave', Swal.resumeTimer)
                                                                            }
                                                                        })

                                                                        Toast.fire({
                                                                            icon: 'success',
                                                                            iconColor: '#70cc72',
                                                                            title: 'Copied "PIN"!',
                                                                            background: '#1a1a1a',
                                                                        })
                                                                        ccicon.style.display = "none";
                                                                    }

                                                                    function toggleshowPW_table() {
                                                                        var show = document.getElementById('PIN-@UserEntry.ID.ToString()');
                                                                        if (show.type == 'password') {
                                                                            show.type = 'text';
                                                                            eyeicontbl.src = "/Elements/eye-slash-white.svg"
                                                                        }
                                                                        else {
                                                                            show.type = 'password'
                                                                            eyeicontbl.src = "/Elements/eye-white.svg"

                                                                        }
                                                                        show.focus();
                                                                        }
                                                                    }

                                                                }
                                                                // Call the function after the document has finished loading
                                                                window.addEventListener('load', initializeCCIcon);
                                                                </script>
                                                            </div>
                                                        </div>
                                                        <!--TIMEOUT INPUT-->
                                                        <div class="mt-3 tbl-details-0" id="tbl-TimeOut-@UserEntry.ID.ToString()">
                                                            <div class="form-floating table-form">
                                                                <p style="margin-bottom:-2px;">ICELock Timeout</p>
                                                                <input type="text" maxlength="50" readonly class="form-control-plaintext readonly" style="width:100%" id="Timeout-@UserEntry.ID.ToString()" value="@UserDetails.TIMEOUT" spellcheck="false">
                                                            </div>
                                                        </div>
                                                    </div>
                                                    <!--DATE INPUT-->
                                                    <div class="col-12 d-flex justify-content-center" style="display: none;">
                                                        <div style="color: gray; margin-right: 60px; margin-bottom: 60px; margin-top: 30px;" id="dateinfo-@UserEntry.ID">
                                                            <p>Created on @UserEntry.DATE_CREATED</p>
                                                        </div>
                                                    </div>
                                                    <!--HIDDEN CONFIRM AND CANCEL BTNS-->
                                                    <div class="col-11 d-flex justify-content-center">
                                                        <div class="mt-4 mb-4" style="color:gray; display:none;" id="editbtns-@UserEntry.ID">
                                                            <button class="btn btn-outline-secondary rounded-0" onclick="updateEntry()" style="margin-right:8px;" aria-pressed="true" id="editacceptbtn">
                                                                CONFIRM
                                                            </button>
                                                            <button class="btn btn-outline-secondary rounded-0" onclick="cancelEdit()" aria-pressed="true" id="editcancelbtn">
                                                                CANCEL
                                                            </button>
                                                        </div>
                                                    </div>
                                                </div>
                                                <nav class="menu" id="edit-menu-@UserEntry.ID">
                                                    <input type="checkbox" href="#" class="menu-open" name="menu-open" id="menu-open-@UserEntry.ID.ToString()" />
                                                    <label class="menu-open-button" for="menu-open-@UserEntry.ID.ToString()">
                                                        <span class="lines line-1"></span>
                                                        <span class="lines line-2"></span>
                                                        <span class="lines line-3"></span>
                                                    </label>
                                                    @*<a class="menu-item red" onclick="deleteEntry()"> <i class="fa fa-trash"></i> </a>*@
                                                    <a class="menu-item green" onclick="restoreUser('@UserEntry.ID','@UserEntry.USERNAME')"> <i class="fa fa-undo"></i> </a>
                                                </nav>
                                            </div>
                                        }
                                    }
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
</body>
</html>

<script type="text/javascript">
    function discardChangesADDITEM() {
        Swal.fire({
            title: 'Cancel changes?',
            text: 'This will dismiss all changes.',
            icon: 'error',
            iconColor: '#c40f2d',
            background: '#1a1a1a',
            showCancelButton: true,
            confirmButtonColor: '#70cc72',
            cancelButtonColor: '#fe4365',
            confirmButtonText: 'Confirm',
            cancelButtonText: 'Back'
        }).then((result) => {
            if (result.isConfirmed) {
                // Handle confirmed action
                $('#addbutton-modal').modal('hide')
            }
        });
    }

    function cancelEdit(PID, Title, Username, Email, Password, Lname, Notes) {
        //Confirmation message before cancelling edit
        Swal.fire({
            title: 'Cancel changes?',
            text: "This will dismiss all changes.",
            icon: 'error',
            iconColor: '#c40f2d',
            background: '#1a1a1a',
            showCancelButton: true,
            confirmButtonColor: '#70cc72',
            cancelButtonColor: '#fe4365',
            confirmButtonText: 'Confirm',
            cancelButtonText: 'Back'
        }).then((result) => {
            if (result.isConfirmed) {
                checkInputemail();
                checkInputusername();
                checkInputpassword();
                checkInputLname();
                checkInputnotes();
                toggleEdit(PID);
                resetform(PID, Title, Username, Email, Password, Lname, Notes);
            }
        })

    }

    function resetform(PID, Title, Username, Email, Password, Lname, Notes) {
        document.getElementById('Title-' + PID).value = Title;
        document.getElementById('Username-' + PID).value = Username;
        document.getElementById('Email-' + PID).value = Email;
        document.getElementById('Password-' + PID).value = Password;
        document.getElementById('Lname-' + PID).value = Lname;
        document.getElementById('Notes-' + PID).value = Notes;
    }

    function toggleEdit(PID) {
        var itemTITLE = document.getElementById('Title-' + PID);
        var itemUN = document.getElementById('Username-' + PID);
        var itemPW = document.getElementById('Password-' + PID);
        var itemEMAIL = document.getElementById('Email-' + PID);
        var itemLname = document.getElementById('Lname-' + PID);
        var itemNOTES = document.getElementById('Notes-' + PID);
        var editicon = document.getElementById('edit-menu-' + PID);
        var editbtns = document.getElementById('editbtns-' + PID);
        var dateinfo = document.getElementById('dateinfo-' + PID);

        //tbldetails
        const UNtblDetail = document.getElementById('tbl-username-' + PID)
        const PWtblDetail = document.getElementById('tbl-password-' + PID)
        const EMAILtblDetail = document.getElementById('tbl-email-' + PID)
        const LnametblDetail = document.getElementById('tbl-Lname-' + PID)
        const NOTEStblDetail = document.getElementById('tbl-notes-' + PID)

        if (itemTITLE.value != null) {
            //toggle
            if (itemUN.classList == 'form-control-plaintext readonly') {
                //REMOVE MENU ICONS
                editicon.style.display = "none";
                dateinfo.style.display = "none";
                //ADD CONFIRM AND CANCEL BUTTONS
                editbtns.style.display = "block";
                //ITEM TITLE
                itemTITLE.readOnly = false;
                itemTITLE.className = "form-control-edit";
                //ITEM UN
                itemUN.readOnly = false;
                itemUN.className = "form-control-edit";
                UNtblDetail.style.display = "block";
                //ITEM PW
                itemPW.readOnly = false;
                itemPW.className = "form-control-edit";
                itemPW.type = 'password';
                PWtblDetail.style.display = "block";
                //ITEM EMAIL
                itemEMAIL.readOnly = false;
                itemEMAIL.className = "form-control-edit";
                EMAILtblDetail.style.display = "block";
                //ITEM Lname
                itemLname.readOnly = false;
                itemLname.className = "form-control-edit";
                LnametblDetail.style.display = "block";
                //ITEM NOTES
                itemNOTES.readOnly = false;
                itemNOTES.className = "form-control-notes-edit form-control-edit";
                NOTEStblDetail.style.display = "block";
            }
            else {
                //ADD MENU ICONS
                editicon.style.display = "block";
                dateinfo.style.display = "block";
                //REMOVE CONFIRM AND CANCEL BUTTONS
                editbtns.style.display = "none";
                //ITEM TITLE
                itemTITLE.readOnly = true;
                itemTITLE.className = "form-control-plaintext form-control-title";
                //ITEM UN
                itemUN.readOnly = true;
                itemUN.className = "form-control-plaintext readonly";
                //ITEM PW
                itemPW.readOnly = true;
                itemPW.className = "form-control-plaintext";
                itemPW.type = 'password';
                //ITEM EMAIL
                itemEMAIL.readOnly = true;
                itemEMAIL.className = "form-control-plaintext";
                //itemEMAIL.value = Email;
                //ITEM Lname
                itemLname.readOnly = true;
                itemLname.className = "form-control-plaintext";
                //ITEM NOTES
                itemNOTES.readOnly = true;
                itemNOTES.className = "form-control-plaintext form-control-notes";
            }
        }
        else {
            Swal.fire({
                position: 'top',
                icon: 'error',
                iconColor: '#c40f2d',
                background: '#1a1a1a',
                title: 'Title is required!',
                showConfirmButton: false,
                timer: 2500
            })
        }

    }
</script>
LUIS
<script type="text/javascript">

    //Function for deleting given entry
    function deleteUser(userID, title) {
        //Confirmation message before deleting entry
        Swal.fire({
            title: 'Ban User?',
            text: "This will permanently disable this user (Until an admin reactives this account)",
            icon: 'warning',
            iconColor: '#c40f2d',
            background: '#1a1a1a',
            showCancelButton: true,
            confirmButtonColor: '#70cc72',
            cancelButtonColor: '#fe4365',
            confirmButtonText: 'Confirm'
        }).then((result) => {
            if (result.isConfirmed) {
                //Packages the received variable
                var data = {
                    userID: userID
                }

                //Ajax function to prepare data to be sent to the controller
                $.ajax({
                    type: "POST",
                    url: "/AdminInterface/BanUser",
                    dataType: "JSON",
                    data: JSON.stringify(data),
                    contentType: 'application/json; charset=utf8',
                    success: function () {
                        Swal.fire({
                            position: 'center',
                            icon: 'success',
                            background: '#1a1a1a',
                            iconColor: '#70cc72',
                            title: '"' + title + '" successfuly banned!',
                            showConfirmButton: false,
                            timer: 2500
                        })
                            .then(function () {
                                location.reload(true);
                            }
                            )
                    },
                    error: function () {
                        alert("An error occurred (Javascript)")
                    }
                })
            }
        })
    }

    //Function for restoring user
    function restoreUser(userID, title) {
        //Confirmation message before deleting entry
        Swal.fire({
            title: 'Unban User?',
            text: "This will restore this user's account",
            icon: 'warning',
            iconColor: '#c40f2d',
            background: '#1a1a1a',
            showCancelButton: true,
            confirmButtonColor: '#70cc72',
            cancelButtonColor: '#fe4365',
            confirmButtonText: 'Confirm'
        }).then((result) => {
            if (result.isConfirmed) {
                //Packages the received variable
                var data = {
                    userID: userID
                }

                //Ajax function to prepare data to be sent to the controller
                $.ajax({
                    type: "POST",
                    url: "/AdminInterface/UnbanUser",
                    dataType: "JSON",
                    data: JSON.stringify(data),
                    contentType: 'application/json; charset=utf8',
                    success: function () {
                        Swal.fire({
                            position: 'center',
                            icon: 'success',
                            background: '#1a1a1a',
                            iconColor: '#70cc72',
                            title: '"' + title + '" successfuly unbanned!',
                            showConfirmButton: false,
                            timer: 2500
                        })
                            .then(function () {
                                location.reload(true);
                            }
                            )
                    },
                    error: function () {
                        alert("An error occurred (Javascript)")
                    }
                })
            }
        })
    }



    //Function for updating given entry

    function updateEntry(userID, userIDstring, title) {

        var itemTITLE = document.getElementById('Title-' + userID);

        //Sets variables to corresponding textboxes
        var uID = userID;
        var uFname = $('#Fname-' + userIDstring).val();
        var uLname = $('#Lname-' + userIDstring).val();
        var uUname = $('#Username-' + userIDstring).val();
        var uEmail = $('#Email-' + userIDstring).val();
        var uPassword = $('#Password-' + userIDstring).val();
        var uPIN = $('#PIN-' + userIDstring).val();
        var uTO = $('#Timeout-' + userIDstring).val()

        //Packages the variables
        var data = {
            userID: uID,
            userFname: uFname,
            userLname: uLname,
            userUsername: uUname,
            userEmail: uEmail,
            userPassword: uPassword,
            userPIN: uPIN,
            userTO: uTO
        }

        //Confirmation message before updating entry
        Swal.fire({
            title: 'Update item?',
            text: "This will update the item.",
            icon: 'question',
            iconColor: '#2973b9',
            background: '#1a1a1a',
            showCancelButton: true,
            confirmButtonColor: '#70cc72',
            cancelButtonColor: '#fe4365',
            confirmButtonText: 'Confirm'
        }).then((result) => {
            if (result.isConfirmed) {
                //Ajax function to prepare data to be sent to the controller
                $.ajax({
                    type: "POST",
                    Lname: "/AdminInterface/UpdateEntry",
                    dataType: "JSON",
                    data: JSON.stringify(data),
                    contentType: 'application/json; charset=utf8',
                    success: function () {

                    },
                    error: function () {
                        alert("An error occurred")
                    }
                })
                Swal.fire({
                    position: 'center',
                    icon: 'success',
                    background: '#1a1a1a',
                    iconColor: '#70cc72',
                    title: '"' + title + '" successfuly updated!',
                    showConfirmButton: false,
                    timer: 2500
                })
                    .then(function () {
                        location.reload(true);
                    }
                    )
            }
        })
    }
</script>